# GOLDEN RULES

절대 위반 금지. 예외 없음.

1. **페이지 하나 = HTML 파일 하나.** CSS·JS는 그 파일 안에 인라인으로 둔다.
   외부 라이브러리·CDN·빌드 도구·패키지 매니저를 추가하지 않는다.
2. **사용자가 입력한 문자열은 `textContent`로만 넣는다.** `innerHTML` 금지.
3. **진짜 비밀값(비밀번호, API 키, 토큰)을 파일에 적지 않는다.** 커밋에도 로그에도.
4. **`README.md`의 `QUOTE:START`~`QUOTE:END` 사이는 손대지 않는다.** GitHub Actions가 매일 자동으로 갈아 끼우는 구간이다.
5. **HTML을 고쳤으면 브라우저로 열어 눈으로 확인한다.** 테스트 스위트가 없으므로 이게 유일한 검증이다.
